services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: typingking
      POSTGRES_USER: typingking
      POSTGRES_PASSWORD: typingking
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
